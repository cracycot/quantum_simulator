# Разделение пользовательских операций и ошибок

## Проблема
Система путала **намеренные пользовательские операции** (гейты) с **ошибками**, которые требуют коррекции.

### Пример неправильного поведения (ДО):
1. Пользователь применяет **X гейт** на q1 (намеренно переворачивает бит)
2. Состояние: `|000⟩ + |111⟩` → `|010⟩ + |101⟩` ✅
3. Система измеряет синдром: обнаруживает расхождение на q1 ❌
4. Система думает: "Ошибка!" ❌
5. Система **исправляет q1 обратно** (применяет X) ❌
6. Результат: пользовательский гейт уничтожен! ❌

## Решение

### Три типа операций:

#### 1. **Пользовательский гейт** (User Gate) 🎯
- Это **намеренная операция**, созданная пользователем
- **НЕ является ошибкой**
- **НЕ требует коррекции**
- Примеры: X, H, Z, CNOT, которые пользователь добавил drag-and-drop
- Показываются с бейджем **"GATES"** (оранжевый)

#### 2. **Gate Error** ⚡
- Это **ошибка**, возникшая **во время выполнения гейта**
- Вероятность задается в настройках: "Вероятность ошибки" (0-20%)
- **Требует коррекции**
- Примеры: 
  - Пользователь применил H, но с вероятностью 5% возникла X-ошибка
  - Показывается красный треугольник ⚠️ на схеме
- Показываются с бейджем **"NOISE/ERROR"** (красный) в истории трансформаций

#### 3. **Noise** 🌊
- Это **внешний шум** (не связан с гейтами)
- Используется в стандартной симуляции (когда нет пользовательских гейтов)
- **Требует коррекции**
- Показывается с бейджем **"NOISE/ERROR"** (красный)

### Новая логика коррекции:

```typescript
applyCustomCircuit(plan: CustomGateStep[]): void {
  // 1. Считаем gate errors ДО применения гейтов
  const gateErrorsCountBefore = system.history.filter(s => s.type === 'gate-error').length;
  
  // 2. Применяем все пользовательские гейты
  for (const step of plan) {
    this.applyCustomGate(step);  // Может вызвать gate error с вероятностью p
  }
  
  // 3. Считаем gate errors ПОСЛЕ применения гейтов
  const gateErrorsCountAfter = system.history.filter(s => s.type === 'gate-error').length;
  const hadGateErrors = gateErrorsCountAfter > gateErrorsCountBefore;
  
  // 4. Применяем коррекцию ТОЛЬКО если были gate errors
  if (hadGateErrors) {
    console.log('Gate errors detected, performing correction');
    measureSyndrome();
    correctErrors();
  } else {
    console.log('No gate errors, skipping correction');
    // Пользовательские гейты применены успешно, коррекция не нужна
  }
}
```

## Примеры использования

### Сценарий 1: Гейты без ошибок (вероятность 0%)

```
Действия пользователя:
1. Установить "Вероятность ошибки" = 0%
2. Добавить X гейт на q1
3. Нажать Play

Результат:
✅ X применен на q1
✅ Состояние изменено: |000⟩+|111⟩ → |010⟩+|101⟩
✅ Синдром измерен (только для информации)
✅ Коррекция НЕ применена (нет gate errors)
✅ Сообщение: "Применен 1 пользовательский гейт без ошибок"
```

### Сценарий 2: Гейты с возможными ошибками (вероятность 10%)

```
Действия пользователя:
1. Установить "Вероятность ошибки" = 10%
2. Добавить H гейт на q0
3. Добавить X гейт на q2
4. Нажать Play

Результат А (90% случаев - без gate errors):
✅ H применен на q0
✅ X применен на q2
✅ Коррекция НЕ применена
✅ Сообщение: "Применены 2 пользовательских гейта без ошибок"

Результат Б (10% случаев - с gate error на одном из гейтов):
✅ H применен на q0
⚡ Gate error после H: дополнительная Z-ошибка на q0
✅ X применен на q2
🔍 Синдром измерен: обнаружена ошибка
✅ Коррекция применена: исправлена Z-ошибка
✅ Сообщение: "Gate errors detected, performing correction"
```

### Сценарий 3: Стандартная симуляция с шумом (без пользовательских гейтов)

```
Действия пользователя:
1. НЕ добавлять гейты
2. Установить "Error Count" = 1
3. Нажать Play

Результат:
✅ Initialize + Encode
🌊 Noise применен (1 X-ошибка на случайный кубит)
🔍 Синдром измерен
✅ Коррекция применена
```

## Визуальные индикаторы

### На квантовой схеме:
- 🟦 **Серый гейт** (H) в секции INIT - кодирование
- 🟧 **Оранжевые точки/CNOT** в секции INIT - создание запутанности
- 🟠 **Красный X** в секции GATES - **пользовательский гейт** (не ошибка!)
- ⚠️ **Красный треугольник** на гейте - **gate error** (это ошибка!)
- 🔴 **Красная операция** в секции NOISE/ERROR - шум или gate error
- 🟩 **Зеленый X** в секции CORR - коррекция ошибки

### В истории трансформаций:
- **ENCODE** (синий) - операции кодирования
- **GATES** (оранжевый) - пользовательские гейты
- **NOISE/ERROR** (красный) - ошибки (gate errors или noise)
- **MEASUREMENT** (фиолетовый) - измерения синдрома
- **CORR** (зеленый) - коррекция ошибок

## Математическая модель

### Пользовательский гейт без gate error:
```
|ψ⟩ → G|ψ⟩
```

### Пользовательский гейт с gate error (вероятность p):
```
С вероятностью (1-p): |ψ⟩ → G|ψ⟩
С вероятностью p:     |ψ⟩ → E·G|ψ⟩  ← Здесь E - это gate error
```

После gate error:
```
🔍 Измерение синдрома: s = Measure(E·G|ψ⟩)
✅ Коррекция: C_s·E·G|ψ⟩ ≈ G|ψ⟩
```

где `C_s` - оператор коррекции, основанный на синдроме `s`.

## Итоговое различие

| Аспект | Пользовательский гейт | Gate Error | Noise |
|--------|----------------------|------------|-------|
| **Природа** | Намеренная операция | Ошибка при выполнении гейта | Внешний шум |
| **Источник** | Пользователь (drag-and-drop) | Вероятностный процесс | Симуляция шума |
| **Требует коррекции?** | ❌ НЕТ | ✅ ДА | ✅ ДА |
| **Секция на схеме** | GATES (оранжевый) | Рядом с гейтом (⚠️) | NOISE/ERROR (красный) |
| **Бейдж в истории** | GATES | NOISE/ERROR | NOISE/ERROR |
| **Контроль** | Полный (выбор гейта) | Вероятность 0-20% | Error Count |

## Файлы изменены

- `src/core/simulator.ts` - изменена логика `applyCustomCircuit()`, добавлена проверка gate errors
- `src/components/ControlPanel.tsx` - добавлена подсказка о коррекции

