# QEC Simulator — Интерактивный симулятор квантовой коррекции ошибок

<p align="center">
  <img src="https://img.shields.io/badge/React-19-blue" alt="React">
  <img src="https://img.shields.io/badge/TypeScript-5.9-blue" alt="TypeScript">
  <img src="https://img.shields.io/badge/Three.js-0.181-green" alt="Three.js">
  <img src="https://img.shields.io/badge/Tailwind-3.4-cyan" alt="Tailwind">
</p>

## 📖 Описание

Веб-приложение для интерактивного изучения принципов **квантовой коррекции ошибок (QEC)**. Симулятор позволяет наглядно увидеть:

- Как логический кубит кодируется в несколько физических кубитов
- Как различные типы ошибок влияют на квантовое состояние
- Как измерение синдромов позволяет обнаружить ошибки
- Как коррекция восстанавливает исходное состояние

## 🎯 Поддерживаемые коды

### 3-кубитный код повторения (Repetition Code)

Простейший код коррекции ошибок. Кодирует 1 логический кубит в 3 физических.

**Кодирование:**
```
|0⟩_L → |000⟩
|1⟩_L → |111⟩
|+⟩_L → (|000⟩ + |111⟩) / √2
```

**Возможности:**
- ✅ Исправляет одиночные X-ошибки (bit-flip)
- ❌ НЕ исправляет Z-ошибки (phase-flip)
- ❌ НЕ исправляет две и более ошибки

**Схема кодирования:**
```
q0 ─────●─────●─────
        │     │
q1 ────⊕─────│─────
              │
q2 ──────────⊕─────
```

### 9-кубитный код Шора (Shor Code)

Первый полноценный код квантовой коррекции ошибок. Кодирует 1 логический кубит в 9 физических.

**Структура:** 3 блока по 3 кубита
- Блок 0: q0, q1, q2
- Блок 1: q3, q4, q5  
- Блок 2: q6, q7, q8

**Возможности:**
- ✅ Исправляет одиночные X-ошибки (bit-flip)
- ✅ Исправляет одиночные Z-ошибки (phase-flip)
- ✅ Исправляет одиночные Y-ошибки (комбинированные)

## 🔧 Архитектура проекта

```
src/
├── core/                    # Ядро симуляции
│   ├── quantum/
│   │   ├── complex.ts       # Комплексные числа и StateVector
│   │   ├── gates.ts         # Квантовые гейты (X, Y, Z, H, CNOT...)
│   │   └── system.ts        # QuantumSystem - управление кубитами
│   ├── codes/
│   │   ├── repetition.ts    # 3-кубитный код повторения
│   │   └── shor.ts          # 9-кубитный код Шора
│   ├── noise/
│   │   ├── noise.ts         # Модели шума
│   │   └── gateErrors.ts    # Ошибки при выполнении гейтов
│   └── simulator.ts         # Главный класс QECSimulator
│
├── components/              # React компоненты
│   ├── BlochSphere.tsx      # 3D визуализация Блох-сферы
│   ├── QuantumCircuit.tsx   # Квантовая схема с drag-and-drop
│   ├── ControlPanel.tsx     # Панель управления (шум/gate errors)
│   ├── EventLog.tsx         # Лог событий с детализацией
│   ├── SyndromeTable.tsx    # Таблица синдромов
│   ├── QBERChart.tsx        # График логических ошибок
│   └── StateDisplay.tsx     # Отображение квантового состояния
│
├── types/
│   └── gatePlan.ts          # Типы для кастомных схем гейтов
│
└── App.tsx                  # Главный компонент приложения
```

## 🧮 Как работает симуляция

### 1. Инициализация
Пользователь выбирает начальное логическое состояние:
- `|0⟩` — базисное состояние "ноль"
- `|1⟩` — базисное состояние "единица"  
- `|+⟩` — суперпозиция `(|0⟩ + |1⟩) / √2`
- `|−⟩` — суперпозиция `(|0⟩ - |1⟩) / √2`

### 2. Кодирование
Логический кубит кодируется в физические кубиты с помощью CNOT-гейтов:

```typescript
// 3-кубитный код
CNOT(q0 → q1)  // Копирование на q1
CNOT(q0 → q2)  // Копирование на q2
// Результат: |ψ⟩ = α|000⟩ + β|111⟩
```

### 3. Шум (ошибки)
Симулятор позволяет выбрать **тип ошибки** и **точное количество** затронутых кубитов:

| Тип | Гейт | Эффект |
|-----|------|--------|
| Bit-flip (X) | X | `|0⟩ ↔ |1⟩` — переворот бита |
| Phase-flip (Z) | Z | `|+⟩ ↔ |−⟩` — переворот фазы |
| Combined (Y) | Y | Комбинация X и Z |

**Количество ошибок:**
- 3-кубитный код: 0, 1 или 2 ошибки
- Код Шора: от 0 до 9 ошибок

Ошибки случайно распределяются по выбранному количеству кубитов.

### 4. Измерение синдрома
Синдром — это результат измерения операторов чётности (стабилизаторов).

**Для 3-кубитного кода:**
- Измеряем `Z₀Z₁` (чётность q0 и q1)
- Измеряем `Z₁Z₂` (чётность q1 и q2)

**Таблица синдромов:**
| Синдром | Ошибка | Коррекция |
|---------|--------|-----------|
| (0, 0) | Нет | — |
| (1, 0) | q0 | X₀ |
| (1, 1) | q1 | X₁ |
| (0, 1) | q2 | X₂ |

### 5. Коррекция
На основе синдрома применяется корректирующий гейт:
```typescript
if (syndrome === [1, 0]) applyGate(X, q0);
if (syndrome === [1, 1]) applyGate(X, q1);
if (syndrome === [0, 1]) applyGate(X, q2);
```

### 6. Проверка
Вычисляется **fidelity** (верность) — мера близости к исходному состоянию:
```
F = |⟨ψ_initial|ψ_final⟩|²
```
- F = 1.0 → идеальное восстановление
- F < 1.0 → есть остаточная ошибка

## 📊 Компоненты визуализации

### Блох-сфера
3D визуализация состояния отдельного кубита:
- **Ось Z:** |0⟩ (верх) и |1⟩ (низ)
- **Ось X:** |+⟩ и |−⟩
- **Ось Y:** |i⟩ и |-i⟩
- **Жёлтая стрелка:** текущее состояние кубита

### Квантовая схема
Отображает последовательность операций:
- 🔴 **X** — bit-flip гейт
- 🔵 **Z** — phase-flip гейт
- 🟣 **Y** — комбинированный гейт
- 🟢 **H** — Hadamard гейт
- 🟠 **CNOT** — контролируемый NOT
- 🟧 **⚡** — ошибка гейта (gate error)
- 🟩 **✓** — успешная коррекция

**Drag-and-drop:**
В режиме Gate Errors можно перетаскивать гейты из палитры прямо на кубитные линии схемы. Для двукубитных гейтов (CNOT, CZ, SWAP) нужно выбрать два кубита последовательно.

### Логический кубит
Показывает fidelity (верность) текущего состояния относительно логических базисных состояний:
- **|0⟩_L** — верность к логическому нулю
- **|1⟩_L** — верность к логической единице
- **Логическая ошибка** — показывает, насколько состояние отклонилось от начального

### График QBER
Показывает теоретическую зависимость логической ошибки от физической:
- **Серая линия** — без коррекции (p_logical = p_physical)
- **Зелёная линия** — 3-кубитный код (p_L ≈ 3p²)
- **Фиолетовая линия** — код Шора (p_L ≈ p²)

### Квантовое состояние
Отображает амплитуды всех базисных состояний:
- Высота столбца — вероятность |амплитуда|²
- Показывает суперпозицию состояний

## 🚀 Запуск проекта локально

### Системные требования

Перед началом установки убедитесь, что на вашем компьютере установлены следующие инструменты:

- **Node.js**: версия 18.0.0 или выше (рекомендуется 20.x LTS)
- **npm**: версия 9.0.0 или выше (поставляется вместе с Node.js)
- **Git**: для клонирования репозитория

#### Проверка установленных версий

Откройте терминал (командную строку) и выполните следующие команды:

```bash
node --version
npm --version
git --version
```

Если какой-либо из инструментов не установлен, выполните установку согласно инструкциям ниже.

### Установка необходимых инструментов

#### Установка Node.js и npm

**macOS:**
```bash
brew install node
```

**Windows:**
1. Скачайте установщик с официального сайта: https://nodejs.org/
2. Запустите установщик и следуйте инструкциям
3. Перезагрузите командную строку после установки

**Linux (Ubuntu/Debian):**
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

**Linux (Fedora/RHEL):**
```bash
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo dnf install nodejs
```

#### Установка Git

**macOS:**
```bash
brew install git
```

**Windows:**
1. Скачайте установщик с: https://git-scm.com/download/win
2. Запустите установщик с настройками по умолчанию

**Linux:**
```bash
sudo apt-get install git
```

### Пошаговая инструкция по развертыванию

#### Шаг 1: Клонирование репозитория

Откройте терминал и выполните команду:

```bash
git clone <URL-вашего-репозитория>
cd project
```

Замените `<URL-вашего-репозитория>` на фактический URL репозитория.

Если у вас нет URL репозитория, а есть архив проекта:

```bash
unzip project.zip
cd project
```

#### Шаг 2: Установка зависимостей

Находясь в корневой директории проекта, выполните:

```bash
npm install
```

Эта команда установит все необходимые пакеты, указанные в `package.json`. Процесс может занять 2-5 минут в зависимости от скорости интернет-соединения.

**Ожидаемый результат:**
- Будет создана папка `node_modules` с установленными зависимостями
- Вы увидите сообщения о прогрессе установки
- В конце появится сообщение о количестве установленных пакетов

**Возможные проблемы:**

1. **Ошибка `EACCES` или проблемы с правами доступа (Linux/macOS):**
```bash
sudo chown -R $USER:$USER ~/.npm
npm install
```

2. **Ошибка `network timeout` или проблемы с сетью:**
```bash
npm config set registry https://registry.npmjs.org/
npm install
```

3. **Устаревший кеш npm:**
```bash
npm cache clean --force
npm install
```

#### Шаг 3: Запуск проекта в режиме разработки

После успешной установки зависимостей запустите проект:

```bash
npm run dev
```

**Ожидаемый результат:**
- В терминале появится сообщение: `VITE v4.x.x ready in XXX ms`
- Будут указаны локальные адреса для доступа к приложению:
  ```
  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.x.x:5173/
  ```
- Проект автоматически откроется в браузере по умолчанию

#### Шаг 4: Открытие приложения в браузере

Если браузер не открылся автоматически:

1. Откройте браузер (рекомендуется Chrome, Firefox, Safari или Edge)
2. Перейдите по адресу: http://localhost:5173/

**Поддерживаемые браузеры:**
- Google Chrome 90+
- Mozilla Firefox 88+
- Safari 14+
- Microsoft Edge 90+

#### Шаг 5: Проверка работоспособности

После открытия приложения вы должны увидеть:
- Интерфейс симулятора квантовой коррекции ошибок
- Панель управления с выбором кода и состояния
- Визуализацию Блох-сферы (3D анимация)
- Квантовую схему

**Тестирование функционала:**
1. Выберите "3-кубитный код"
2. Выберите начальное состояние "|0⟩"
3. Перейдите во вкладку "Шум"
4. Выберите тип ошибки "X (bit-flip)"
5. Установите количество ошибок: 1
6. Нажмите кнопку Play (▶️)
7. Вы должны увидеть пошаговую симуляцию процесса коррекции ошибок

### Остановка сервера разработки

Чтобы остановить работающий сервер разработки:

1. Перейдите в терминал, где запущен `npm run dev`
2. Нажмите `Ctrl+C` (или `Cmd+C` на macOS)
3. Подтвердите остановку, если потребуется (Y/Yes)

### Сборка для продакшена

Если вам нужно создать оптимизированную версию для развертывания на сервере:

#### Шаг 1: Создание production-сборки

```bash
npm run build
```

**Ожидаемый результат:**
- В корне проекта будет создана папка `dist`
- Внутри `dist` будут оптимизированные файлы HTML, CSS и JavaScript
- В терминале появится информация о размере собранных файлов

#### Шаг 2: Предварительный просмотр production-сборки

Чтобы проверить собранную версию локально:

```bash
npm run preview
```

Приложение будет доступно по адресу: http://localhost:4173/

#### Шаг 3: Развертывание на хостинге

Содержимое папки `dist` можно развернуть на любом статическом хостинге:

**Netlify:**
```bash
npm install -g netlify-cli
netlify deploy --dir=dist --prod
```

**Vercel:**
```bash
npm install -g vercel
vercel --prod
```

**GitHub Pages:**
1. Убедитесь, что в `vite.config.ts` указан правильный `base`:
```typescript
export default defineConfig({
  base: '/название-репозитория/',
  ...
})
```
2. Создайте сборку: `npm run build`
3. Опубликуйте содержимое `dist` в ветку `gh-pages`

**Обычный веб-сервер (Apache/Nginx):**
- Скопируйте содержимое папки `dist` в корневую директорию веб-сервера
- Настройте перенаправление всех запросов на `index.html` для поддержки SPA

### Настройка переменных окружения (опционально)

Если проект использует переменные окружения, создайте файл `.env.local` в корне проекта:

```bash
cp .env.example .env.local
```

Отредактируйте `.env.local` и укажите необходимые значения.

### Структура проекта

```
project/
├── src/                    # Исходный код приложения
│   ├── core/              # Ядро симуляции (квантовая логика)
│   ├── components/        # React компоненты UI
│   ├── types/             # TypeScript типы
│   ├── App.tsx            # Главный компонент
│   └── main.tsx           # Точка входа
├── tests/                 # Тесты
├── public/                # Статические файлы
├── dist/                  # Собранная версия (создается после build)
├── package.json           # Зависимости и скрипты
├── vite.config.ts         # Конфигурация Vite
├── tsconfig.json          # Конфигурация TypeScript
└── README.md              # Документация

```

### Доступные npm-скрипты

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск сервера разработки с hot-reload |
| `npm run build` | Создание production-сборки в папку `dist` |
| `npm run preview` | Предварительный просмотр production-сборки |
| `npm run lint` | Проверка кода на ошибки с помощью ESLint |

### Решение частых проблем

#### Проблема: Порт 5173 уже занят

**Решение:**
```bash
npx kill-port 5173
npm run dev
```

Или укажите другой порт:
```bash
npm run dev -- --port 3000
```

#### Проблема: Не работает hot-reload (изменения не применяются автоматически)

**Решение:**
1. Остановите сервер (Ctrl+C)
2. Очистите кеш:
```bash
rm -rf node_modules/.vite
npm run dev
```

#### Проблема: Белый экран или ошибка в браузере

**Решение:**
1. Откройте консоль разработчика (F12)
2. Проверьте вкладку Console на наличие ошибок
3. Попробуйте очистить кеш браузера (Ctrl+Shift+Delete)
4. Переустановите зависимости:
```bash
rm -rf node_modules package-lock.json
npm install
npm run dev
```

#### Проблема: Ошибки TypeScript при сборке

**Решение:**
```bash
npx tsc --noEmit
```

Эта команда покажет все ошибки TypeScript без создания сборки.

#### Проблема: Приложение работает медленно

**Рекомендации:**
1. Используйте современный браузер (последняя версия Chrome/Firefox)
2. Закройте другие вкладки браузера
3. Для code Шора (9 кубитов) производительность может быть ниже из-за больших вычислений
4. Попробуйте production-сборку: `npm run build && npm run preview`

### Системные требования для запуска

**Минимальные:**
- CPU: Intel Core i3 или аналог
- RAM: 4 GB
- Браузер: Chrome 90+, Firefox 88+, Safari 14+

**Рекомендуемые:**
- CPU: Intel Core i5 или выше
- RAM: 8 GB или более
- Браузер: Последняя версия Chrome/Firefox
- GPU с поддержкой WebGL 2.0 (для 3D визуализации Блох-сферы)

### Дополнительная информация

Для более детальной информации о функциональности и использовании симулятора обратитесь к следующим файлам в проекте:

- `QUICK_START.md` - Быстрое введение в работу с симулятором
- `TRANSFORMATION_VIEW_GUIDE.md` - Руководство по просмотру трансформаций состояний
- `USER_GATES_FIX.md` - Работа с пользовательскими гейтами
- `TESTING_CHECKLIST.md` - Чек-лист тестирования функциональности

### Поддержка и обратная связь

Если у вас возникли проблемы, которые не удалось решить с помощью этой инструкции:

1. Проверьте раздел Issues в репозитории проекта
2. Создайте новый Issue с подробным описанием проблемы
3. Укажите версию Node.js, npm, операционную систему и браузер

---

**Автор:** ITMO University • Physics Project • 2025

## 🎮 Как пользоваться

### Режим «Шум» (базовый сценарий)
1. **Выберите код:** 3-кубитный или код Шора
2. **Выберите начальное состояние:** |0⟩, |1⟩, |+⟩ или |−⟩
3. **Перейдите во вкладку «Шум»**
4. **Выберите тип ошибки:** X (bit-flip), Z (phase-flip) или Y (combined)
5. **Выберите количество ошибок:** 0, 1 или 2
6. **Нажмите Play (▶️)** для запуска симуляции
7. **Используйте кнопки ◀️ и ▶️** для пошаговой навигации

### Режим «Gate Errors» (ошибки гейтов)

Этот режим позволяет создавать собственные квантовые схемы и настраивать вероятность ошибки для каждого гейта.

1. **Перейдите во вкладку «Gate Errors»**
2. **Перетащите гейты из палитры** на квантовую схему:
   - Однокубитные: H, X, Y, Z, Rx, Ry, Rz, S, T
   - Двукубитные: CNOT, CZ, SWAP (требуют выбора двух кубитов)
3. **Кликните на гейт на схеме** для настройки:
   - Вероятность ошибки (0-20%)
   - Тип ошибки (X, Z, Y, Depolarizing)
4. **Нажмите «Запустить схему»** для выполнения

**Что такое Gate Error?**

В реальных квантовых компьютерах гейты не идеальны. При каждом применении гейта есть вероятность возникновения дополнительной ошибки. Например:
- При применении CNOT с вероятностью 2% может возникнуть X-ошибка на одном из кубитов
- Это моделирует реальные физические несовершенства оборудования

**Визуализация:**
- ⚡ Ошибки гейтов отображаются оранжевым цветом с молнией
- В логе событий можно раскрыть детали каждой ошибки
- Счётчик ошибок показывает общее количество gate errors

### Эксперименты

#### Эксперимент 1: Одиночная X-ошибка (успешная коррекция)
1. Выберите 3-кубитный код, состояние |0⟩
2. Тип ошибки: X, количество: 1
3. Запустите симуляцию
4. ✅ Синдром укажет на ошибочный кубит
5. ✅ Коррекция восстановит состояние (логическая ошибка ≈ 0%)

#### Эксперимент 2: Две X-ошибки (предел кода)
1. Выберите 3-кубитный код, состояние |0⟩
2. Тип ошибки: X, количество: 2
3. Запустите симуляцию
4. ⚠️ Синдром укажет на НЕПРАВИЛЬНЫЙ кубит
5. ❌ Коррекция ухудшит состояние (логическая ошибка = 100%)

#### Эксперимент 3: Z-ошибка на 3-кубитном коде
1. Выберите 3-кубитный код, состояние |+⟩
2. Тип ошибки: Z, количество: 1
3. Запустите симуляцию
4. ⚠️ Синдром (0,0) — ошибка НЕ обнаружена!
5. ❌ 3-кубитный код не защищает от phase-flip

#### Эксперимент 4: Код Шора vs Z-ошибка
1. Выберите код Шора, состояние |+⟩
2. Тип ошибки: Z, количество: 1
3. Запустите симуляцию
4. ✅ Phase-синдром обнаружит ошибку
5. ✅ Код Шора успешно исправляет Z-ошибки

#### Эксперимент 5: Gate Errors (несовершенные гейты)
1. Перейдите во вкладку «Gate Errors»
2. Перетащите несколько H-гейтов на схему
3. Кликните на каждый гейт, установите вероятность ошибки 10%
4. Нажмите «Запустить схему»
5. ⚡ Наблюдайте появление случайных ошибок при выполнении гейтов
6. 📊 Проанализируйте лог событий — раскройте детали ошибок

## 📚 Теоретическая справка

### Квантовые гейты

| Гейт | Матрица | Действие |
|------|---------|----------|
| X | `[[0,1],[1,0]]` | Поворот на π вокруг оси X |
| Y | `[[0,-i],[i,0]]` | Поворот на π вокруг оси Y |
| Z | `[[1,0],[0,-1]]` | Поворот на π вокруг оси Z |
| H | `[[1,1],[1,-1]]/√2` | Создание суперпозиции |
| CNOT | 4×4 матрица | Условный NOT |

### Стабилизаторы

Стабилизатор — оператор S, для которого S|ψ⟩ = |ψ⟩ для всех кодовых состояний.

**3-кубитный код:**
- S₁ = Z₀Z₁ (проверяет чётность первых двух кубитов)
- S₂ = Z₁Z₂ (проверяет чётность последних двух)

### Порог ошибки

Код эффективен когда:
```
p_logical < p_physical
```

Для 3-кубитного кода: `3p² < p` → `p < 1/3 ≈ 33%`

На практике порог обычно ~10-15% из-за накопления ошибок.

## 🛠 Технологии

- **React 19** — UI фреймворк
- **TypeScript** — типизация
- **Three.js + React Three Fiber** — 3D визуализация
- **Framer Motion** — анимации
- **Recharts** — графики
- **Tailwind CSS** — стили
- **Vite** — сборщик

## 📄 Лицензия

MIT License — свободное использование для образовательных целей.

## 👥 Авторы

ITMO University • Physics Project • 2025
