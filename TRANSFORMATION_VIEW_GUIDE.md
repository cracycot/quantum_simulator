# 📜 Руководство по Истории Трансформаций

## Что нового?

Добавлен новый компонент **"История трансформаций"**, который показывает детальную эволюцию квантового состояния на каждом шаге симуляции.

## Где найти?

В интерфейсе симулятора появилась новая раскрываемая секция **"📜 История трансформаций"** (между "Квантовое состояние" и "Блох-сферы").

## Что отображается?

Для каждого шага симуляции показывается:

### 1. **Заголовок операции**
- Номер шага (#1, #2, ...)
- Тип операции (GATE, ENCODE, NOISE, CORRECTION, MEASUREMENT)

### 2. **Информация об операции**
- Иконка (🌀 суперпозиция, 🔗 запутанность, ⚠️ ошибка, ✅ коррекция, 📏 измерение)
- Название гейта и кубиты, к которым он применен

### 3. **Трансформация состояния**
```
До:    0.7071|000⟩ + 0.7071|111⟩
  →
После: 0.7071|001⟩ + 0.7071|110⟩
```

### 4. **Физический смысл**
💡 Понятное объяснение того, что произошло:
- "Создана суперпозиция на q0"
- "Создана запутанность между q0 и q1"
- "Внесена X-ошибка на q1"
- "Применена X-коррекция на q1"

## Типы эффектов и их иконки

| Эффект | Иконка | Описание |
|--------|--------|----------|
| Суперпозиция | 🌀 | Hadamard-гейт создает суперпозицию |
| Запутанность | 🔗 | CNOT связывает кубиты |
| Ошибка | ⚠️ | Шум или gate error |
| Коррекция | ✅ | Исправление ошибки |
| Измерение | 📏 | Измерение синдрома |
| Кодирование | 🔐 | Кодирование логического состояния |
| Декодирование | 🔓 | Декодирование в логическое состояние |

## Цветовая индикация

Левая граница каждого шага окрашена в цвет, соответствующий типу операции:
- **Фиолетовый градиент** - Суперпозиция
- **Розовый градиент** - Запутанность
- **Красно-желтый градиент** - Ошибка
- **Сине-фиолетовый градиент** - Коррекция
- **Голубой градиент** - Измерение

## Как использовать для защиты лабы

### Сценарий 1: Демонстрация работы 3-кубитного кода

1. Выберите "3-qubit repetition code"
2. Выберите начальное состояние (например, |+⟩)
3. Включите шум "Bit-flip" с вероятностью 10%
4. Запустите симуляцию
5. Откройте "📜 История трансформаций"

**Что показывать:**
```
Шаг 1: Initialize
💡 Инициализация начального состояния
До:    |0⟩
После: |0⟩

Шаг 2: H → q0
💡 Создана суперпозиция на q0
До:    |0⟩
После: 0.7071|0⟩ + 0.7071|1⟩

Шаг 3: CNOT → q0,q1
💡 Создана запутанность между q0 и q1
До:    (|0⟩+|1⟩)/√2 ⊗ |0⟩
После: (|00⟩+|11⟩)/√2

Шаг 4: CNOT → q0,q2
💡 Создана запутанность между q0 и q2
До:    (|00⟩+|11⟩)/√2 ⊗ |0⟩
После: (|000⟩+|111⟩)/√2

Шаг 5: X → q1 (noise)
💡 Внесена X-ошибка на q1
До:    (|000⟩+|111⟩)/√2
После: (|010⟩+|101⟩)/√2

Шаг 6: Syndrome measurement
💡 Измерение синдрома
Результат: (1, 1)

Шаг 7: X → q1 (correction)
💡 Применена X-коррекция на q1
До:    (|010⟩+|101⟩)/√2
После: (|000⟩+|111⟩)/√2
```

### Сценарий 2: Демонстрация Gate Errors

1. Переключитесь на вкладку "Gate Errors"
2. Перетащите H-гейт на q0
3. Установите вероятность ошибки 100%
4. Запустите схему
5. Откройте "📜 История трансформаций"

**Что показывать:**
```
Шаг 2: H → q0
💡 Создана суперпозиция на q0

GATE-ERROR: X на q0 после гейта H
💡 Внесена X-ошибка на q0
```

### Сценарий 3: Объяснение кодирования Шора

1. Выберите "Shor (9-qubit)"
2. Запустите симуляцию
3. Откройте "📜 История трансформаций"

Видно последовательность:
- Применение H на первый кубит → создание суперпозиции
- CNOT 0→1, 0→2 → создание запутанности в первом блоке
- H на каждый из трех блоков → фазовое кодирование
- CNOT внутри блоков → запутанность для фазовой коррекции

## Что говорить на защите

**Вопрос:** "Как происходит кодирование?"

**Ответ:** "Давайте посмотрим на Историю трансформаций. Сначала мы применяем Hadamard-гейт к первому кубиту, это создает суперпозицию. Потом CNOT от q0 к q1 — это создает запутанность между этими кубитами. Следующий CNOT от q0 к q2 запутывает все три кубита. Теперь у нас логический кубит представлен тремя физическими кубитами в запутанном состоянии."

**Вопрос:** "Как обнаруживаются ошибки?"

**Ответ:** "Смотрите, после внесения ошибки X на q1, состояние изменилось с (|000⟩+|111⟩)/√2 на (|010⟩+|101⟩)/√2. Затем мы измеряем синдром — это показывает, что паттерн нарушен. Синдром (1,1) однозначно указывает на ошибку на q1."

**Вопрос:** "Что такое запутанность?"

**Ответ:** "Вот здесь, на шаге с CNOT, вы видите трансформацию. До: кубиты были независимы, каждый в своем состоянии. После: состояние всей системы нельзя разложить на независимые кубиты. Если измерим q0 и получим 0, то q1 тоже будет 0. Если q0=1, то и q1=1. Это и есть запутанность."

## Технические детали

### Что изменилось в коде

1. **Расширен интерфейс `QuantumStep`:**
```typescript
interface StateTransformation {
  simplifiedBefore: string;
  simplifiedAfter: string;
  physicalMeaning: string;
  effect: 'superposition' | 'entanglement' | 'error' | 'correction' | ...;
  icon: string;
}

interface QuantumStep {
  // ... existing fields
  transformation?: StateTransformation;
}
```

2. **Добавлены методы в `QuantumSystem`:**
- `simplifyState()` - упрощает представление состояния
- `generateTransformation()` - создает описание трансформации для гейтов
- `generateStepTransformation()` - для других типов операций

3. **Обновлены методы логирования:**
- `applyGate()` - теперь сохраняет трансформации
- `logStep()` - генерирует трансформации для всех типов
- `applyGatesWithDescription()` - для составных операций
- `measureQubit()` - для измерений

4. **Новый компонент `TransformationView`:**
- Отображает историю с красивой визуализацией
- Фильтрует только шаги с трансформациями
- Цветовая индикация по типам
- Прокручиваемый список

## Преимущества для демонстрации

✅ **Наглядность** - видно ЧТО меняется на каждом шаге
✅ **Понятность** - физический смысл операций
✅ **Образовательность** - можно объяснить каждый шаг
✅ **Профессионализм** - хорошая визуализация
✅ **Полнота** - вся история симуляции в одном месте

## Примеры для презентации

### Слайд 1: "Что такое квантовая коррекция ошибок?"
Покажите историю трансформаций для полного цикла encode → error → correction

### Слайд 2: "Как работает 3-кубитный код?"
Покажите создание запутанности через CNOT

### Слайд 3: "Обнаружение ошибок"
Покажите изменение состояния до и после ошибки, синдром

### Слайд 4: "Исправление ошибок"
Покажите восстановление исходного состояния после коррекции

---

**Итог:** История трансформаций превращает симулятор в образовательный инструмент, где каждая операция не просто выполняется, но и объясняется!

